# 📚 Gestion de Bibliothèque en Ligne

## 📖 Description

Application web full-stack permettant la gestion d'une bibliothèque en ligne.

Elle assure :

- L'inscription et la gestion des adhérents
- La gestion du catalogue de livres
- Le suivi des emprunts et retours
- Une interface moderne et responsive
- Un déploiement simplifié via Docker

Ce projet a été réalisé dans un objectif d’apprentissage, de mise en pratique des architectures full-stack modernes et d’intégration DevOps.

---

## 🎯 Objectifs du Projet

- Concevoir une API REST robuste avec Flask
- Implémenter une interface moderne avec React + TailwindCSS
- Connecter une base de données PostgreSQL
- Containeriser l’application avec Docker

---

## 🛠 Stack Technique

### 🔹 Frontend
- React.js
- Vite
- TailwindCSS
- Shadcn UI

### 🔹 Backend
- Python 3.12
- Flask
- Flask-SQLAlchemy
- Flask-Migrate
- Gunicorn

### 🔹 Base de données
- PostgreSQL

### 🔹 DevOps
- Docker
- Docker Compose

---

## 🏗 Architecture du Projet
library_management_app/
│
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── app.py
│
├── frontend/
│   ├── Dockerfile
│   ├── package.json
│
├── docker-compose.yml
└── README.md

## 🚀 Installation & Exécution
- Méthode 1 — Avec Docker (Recommandé)
* Cloner le projet
git clone https://github.com/ChubyMonster/library_management_app.git
cd library_management_app

- Lancer l’application
docker compose up --build

- Accès à l’application
 Frontend : http://localhost:5173
 Backend API : http://localhost:8000
 PostgreSQL : localhost:5432

- Méthode 2 — Installation Locale
   Backend
    Créer un environnement virtuel
    cd backend
    python -m venv venv
    venv\Scripts\activate   # Windows

- Installer les dépendances
  pip install -r requirements.txt

- Configurer la base de données

- Créer une base PostgreSQL :
library_management_app
****************************************************************
-- 1. Table: Profil
CREATE TABLE Profil (
    ID_profil INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nom_P VARCHAR(100),
    description_p VARCHAR(255)
);

-- 2. Table: Utilisateur
CREATE TABLE Utilisateur (
    ID_User INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Login VARCHAR(100),
    Password VARCHAR(100),
    Profil_ID INT,
    Mbre_ID INT,
    FOREIGN KEY (Profil_ID) REFERENCES Profil(ID_profil)
    -- Note: Relationship to Membre depends on business logic,
    -- often added later via ALTER TABLE or handled via application logic.
);

-- 3. Table: Categorie
CREATE TABLE Categorie (
    ID_cat INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nom_cat VARCHAR(100),
    Champ VARCHAR(100)
);

-- 4. Table: Auteur
CREATE TABLE Auteur (
    ID_auteur INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nom_auteur VARCHAR(100),
    Prenom_auteur VARCHAR(100)
);

-- 5. Table: Livre
CREATE TABLE Livre (
    ID_Livre INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ISBN VARCHAR(20),
    Titre VARCHAR(255),
    Quantite INT,
    Cat_ID INT,
    FOREIGN KEY (Cat_ID) REFERENCES Categorie(ID_cat)
);

-- 6. Table: Livre_Auteur (Associative table for Many-to-Many)
CREATE TABLE Livre_Auteur (
    Livre_ID INT,
    Auteur_ID INT,
    PRIMARY KEY (Livre_ID, Auteur_ID),
    FOREIGN KEY (Livre_ID) REFERENCES Livre(ID_Livre),
    FOREIGN KEY (Auteur_ID) REFERENCES Auteur(ID_auteur)
);

-- 7. Table: Membre
CREATE TABLE Membre (
    ID_mbre INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Nom_mbre VARCHAR(100),
    Prenom_mbre VARCHAR(100),
    Email_mbre VARCHAR(100),
    Date_adhesion DATE
);

-- 8. Table: Emprunt
CREATE TABLE Emprunt (
    ID_emprunt INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Livre_ID INT,
    Membre_ID INT,
    Date_emprunt DATE,
    Date_retour DATE,
    FOREIGN KEY (Livre_ID) REFERENCES Livre(ID_Livre),
    FOREIGN KEY (Membre_ID) REFERENCES Membre(ID_mbre)
);
****************************************************************
- Configurer la variable d’environnement :
set DATABASE_URL=postgresql://postgres:admin@localhost:5432/library_management_app

- Lancer le serveur
flask run

- Frontend
cd frontend
npm install
npm run dev

- Frontend disponible sur :
    http://localhost:5173

- Fonctionnalités principales

✔ Inscription des adhérents
✔ Ajout / modification / suppression des livres
✔ Emprunt et retour des livres
✔ Interface moderne et responsive
✔ API REST sécurisée et structurée

API REST

- L’API backend expose des endpoints REST pour :
Gestion des livres
Gestion des utilisateurs
Gestion des emprunts

- Améliorations Futures :

 Authentification JWT
 Gestion des rôles (Admin / Adhérent)
 Tableau de bord statistique
 Export PDF des emprunts
 Déploiement cloud (Render / Railway / AWS)

 Auteur :
 Issam SGHIOUAR, Smail DRIBI, Râbia BENBRAHIM, Yousra BELLA, Ayoub BERNANNOU
 Master d'Université - Génie Logiciel & Systèmes Intelligents
 École Nationale des Sciences Appliquées de Fès
 
